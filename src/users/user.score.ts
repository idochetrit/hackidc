class UserScore {
  
}
// import _ from "lodash";

// export default (() => {
//   const WEIGHTS = {
//     isStudent: 0.1
//   };

//   function normalizeFields({ isStudent }) {
//     return { isStudent };
//   }

//   class UserScore {
//     calculateScore(user) {
//       const { isStudent } = user;
//       const normals = normalizeFields({ isStudent });

//       const isStudentScore = normals.isStudent * WEIGHTS.isStudent;
//       return _.sum(isStudentScore);
//     }
//   }

//   return new UserScore();
// })();
