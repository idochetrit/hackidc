const generator = {
  methods: {
    generateTeamID() {
      //make a HTML request to get a new generated team-id when team builder option selected
      //inject to this.userData.teamId
    }
  }
}

export default generator